<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include("partials/header.ejs") %>

        <div class="edit-container">

            <h1>Edit Book</h1>
            <form action="/edit" method="post">
                <input type="hidden" name="editedBook" value="<%= book.book_id %>">

                

                <div class="book-conainer">

                    <div class="image_cover"> 
                        <img src="<%= book.cover_image %>" alt="<%= book.title + "cover image" %>">
                    </div>

                    <div class="title inputs-container">
                        <lable for="title">Book Title:</label>
                        <input type="text" id="title" name="title" value="<%= book.title %>" required>
                    </div>

                    <div class="recomendation-and-rating-container">
                        <div class="rating rr inputs-container">
                            <label for="rating">Rating</label>
                            <input type="number" id="rating" name="rating" min="1" max="5" step="1" value="<%= book.rating %>" required>
                        </div>

                        <div class="date-part">
                            <label for="date_read">Date Read:</label>
                            <input type="date" id="date_read" name="date_read" value="<%=book.date_read %>" required>
                        </div>

                        <div class="recommendation rr inputs-container">
                            <label for="recommendation ">Recommendation</label>
                            <input type="number" id="recommendation" name="recommendation" min="1" max="10" value="<%= book.recomendation %>" required>
                        </div>
                    </div>

                <div class="revew-container">

                        <div class="short_review inputs-container">
                        <label for="short_review">Short Review:</label>
                        <textarea id="short_review" name="short_review" rows="7" required><%= book.short_review %></textarea>
                    </div>

                    <div class="long_review inputs-container">
                        <label for="long_review">Long Review:</label>
                        <textarea id="long_review" name="long_review" rows="9" required><%= book.long_review %></textarea>
                    </div>
                </div>

                    <div class="edit_button">
                    <button type="submit">Update Book</button>
                </div>

                </div>

                

            </form>
        </div>


    <%- include("partials/footer.ejs") %>
</body>
</html>