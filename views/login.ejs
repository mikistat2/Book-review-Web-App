<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up | Book Review Web App</title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="icon" type="image/png" href="/images/header-img.png">
  <style>
    /* Prevent horizontal scroll */
    html, body {
      margin: 0;
      padding: 0;
      width: 100%;
      max-width: 100vw;
      overflow-x: hidden;
      font-family: 'Montserrat', sans-serif;
    }

    /* Flex container */
    .signup-flex-container {
      display: flex;
      min-height: 100vh;
      width: 100%;
      max-width: 100vw;
      justify-content: center;
      align-items: center;
       
    }

    /* Left panel */
    .signup-left {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100%;
    }
    .signup-left img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    /* Right panel */
    .signup-right {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    /* Glassmorphism Card */
    .signup-form-card {
      width: 100%;
      max-width: 380px;
      background: rgba(255,255,255,0.2);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.25);
      padding: 40px 30px;
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border: 1px solid rgba(255,255,255,0.3);
    }

    .signup-form-card h2 {
      text-align: center;
      color: #222b45;
      font-weight: 700;
      font-size: 2rem;
      margin-bottom: 10px;
      letter-spacing: 0.5px;
    }
    .signup-form-card p {
      text-align: center;
      color: #3b3b4f;
      margin-bottom: 20px;
    }

    /* Form elements */
    .signup-form-card label {
      font-weight: 600;
      color: #2d3a4a;
      margin-bottom: 6px;
      display: block;
    }
    .signup-form-card input {
      padding: 12px;
      border-radius: 10px;
      border: 1.5px solid #c7d2fe;
      background: #f4f7ff;
      font-size: 1rem;
      margin-bottom: 16px;
      width: 100%;
      color: #23234c;
      box-sizing: border-box;
      transition: border 0.2s, background 0.2s;
    }
    .signup-form-card input:focus {
      border: 1.5px solid #fff;
      outline: none;
      background: rgba(255,255,255,0.25);
    }

    /* Primary button */
    .signup-form-card button {
      margin-top: 10px;
      padding: 14px 0;
      border-radius: 12px;
      border: none;
      background: linear-gradient(to right, #8e2de2, #4a00e0);
      color: #fff;
      font-size: 1.1rem;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 3px 12px rgba(0,0,0,0.3);
      transition: background 0.3s, transform 0.2s;
      width: 100%;
    }
    .signup-form-card button:hover {
      background: linear-gradient(to right, #4a00e0, #8e2de2);
      transform: translateY(-2px);
    }

    /* Google button */
    .google-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      background: #fff;
      color: #444;
      border: none;
      border-radius: 10px;
      font-weight: 600;
      font-size: 1rem;
      padding: 12px 0;
      margin-top: 12px;
      cursor: pointer;
      transition: box-shadow 0.2s;
      width: 100%;
      box-shadow: 0 2px 6px rgba(0,0,0,0.15);
    }
    .google-btn img {
      width: 20px;
      height: 20px;
    }
    .google-btn:hover {
      box-shadow: 0 3px 10px rgba(0,0,0,0.25);
    }

    /* Login link */
    .signup-form-card .login-link {
      display: block;
      text-align: center;
      margin-top: 18px;
      color: #2d3a4a;
      text-decoration: underline;
      font-size: 0.95rem;
      transition: color 0.2s;
    }
    .signup-form-card .login-link:hover {
      color: #4a00e0;
    }

    /* Responsive tweaks */
   /* Tablet and smaller (<= 900px) */
@media (max-width: 900px) {
  .signup-flex-container {
    flex-direction: column;
  }
  .signup-left {
    display: none; /* hide left side */
  }
  .signup-right {
    width: 100%;
  }
}

  </style>
</head>
<body>
  <%- include("partials/header.ejs") %>
  <div class="signup-flex-container slide-in">
    <div class="signup-left">
      <img style="margin-left: 20px; min-height: 600px; border-radius: 20px;" src="https://i0.wp.com/pixahive.com/wp-content/uploads/2020/11/Book-aesthetic-208274-pixahive.jpg?fit=1560%2C1081&ssl=1">
    </div>
    <div class="signup-right">
      <div class="signup-form-card">
        <h2>Login</h2>
        <p>Welcome back!</p>

        <form action="/login" method="POST">
          <label for="login">Username or Email</label>
          <% if (message && message.length > 0 && !message[0].includes("password")) { %>
            <div class="error-message" style="color: #b71c1c; font-size: 0.98rem; margin: 6px 0 6px 0; text-align: left;">
              <%= message[0] %>
            </div>
          <% } %>
          <input type="text" id="login" name="login" placeholder="Enter your username or email" autocomplete="username" required>
          
          <label for="password">Password</label>
          <% if (message && message.length > 0 && message[0].toLowerCase().includes("password")) { %>
            <div class="error-message" style="color: #b71c1c; font-size: 0.98rem; margin: 6px 0 6px 0; text-align: left;">
              <%= message[0] %>
            </div>
          <% } %>
          <input type="password" id="password" name="password" placeholder="Enter your password" autocomplete="current-password" required>
          <button type="submit">Login</button>
        </form>

        <form action="/auth/google" method="GET">
          <button type="submit" class="google-btn">
            <i class="fa-brands fa-google" style="font-size: 1.3em; color: #ea4335;"></i>
            Continue with Google
          </button>
        </form>

        <a class="login-link" href="/signup">Dont have an account? signup</a>
      </div>
    </div>
  </div>
  <%- include("partials/footer.ejs") %>
</body>
</html>
